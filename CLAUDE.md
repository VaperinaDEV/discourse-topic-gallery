# Discourse plugin

## Architecture

This project is a discourse plugin that will, at the end, show a gallery of all pictures from a given topic.

## Project Structure
Project is located at discourse/plugins/discourse-topic-gallery
Primary sources of information should be :
- /discourse/app/controllers
- /discourse/app/helpers
- /discourse/app/models
- /discourse/app/serializers
- /discourse/app/views

Only browse necessary files required to solve a given issue. Minimize text output, be straightforward. Don't sugarcoat. Don't overcomment the code and files you'll create.